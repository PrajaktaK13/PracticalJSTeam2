// export/import allow classes to be shared between files;
export class Animal1 {   // means we're exporting the Animal class so another file can use it.

    eat() {
        console.log("Animal is eating");
    }
}